let docBlock = document.querySelectorAll('.doc').length;
let annotationBlock = document.querySelectorAll('.annotation').length;

for (let n = 0; n < docBlock.lenth; n++) {
    let documentBlock = docBlock[n];
}

for (let m = 0; m < annotationBlock.length; m++) {
    let annotation = annotationBlock[m];
}